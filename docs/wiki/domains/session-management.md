# Domain: Session Management

> Auto-generated by Secrin · 2026-02-24

**Subdomain:** Sessions

Handles user sessions, including creating and managing sessions.

## Functions

- **`get_user_settings`** — [`backend/crud_settings.py`](../modules/backend.md)
  _该函数从数据库中获取指定用户的用户设置信息，返回一个 `models.UserSettings` 对象或 `None`，表示没有找到匹配的记录。_
- **`upsert_user_settings`** — [`backend/crud_settings.py`](../modules/backend.md)
  _该函数用于更新或创建用户设置，根据传入的 `user_id` 和 `settings_in` 参数获取当前用户的设置，并将新的设置保存到数据库中。如果用户已经存在，则更新现有设置；否则，创建一个新的设置。_
- **`get_user_themes`** — [`backend/crud_themes.py`](../modules/backend.md)
  _该函数从数据库中获取指定用户的所有主题信息，并返回这些主题的列表。_
- **`get_user_settings`** — [`backend/routers/users.py`](../modules/backend.md)
  _该函数用于获取用户设置信息，包括但不限于用户的ID、用户名、邮箱等。它依赖于 `get_current_user_id` 和 `get_db` 函数来获取当前登录的用户ID和数据库连接对象。  ### 2-3 Sentence Summary:  1. **功能描述**：该函数从数据库中获取指定用户设置的信息。 2. **参数说明**：    - `current_user_id`: 获取当前登录用户的ID。    - `db`: 获取数据库连接对象，用于执行数据库操作。 3. **返回类型**：返回一个包含用户设置信息的响应模型。  ### 4. Why It Exists:  - **数据获取**：通过 `get_current_user_id` 和 `get_db` 函数，可以获取当前登录用户的ID和数据库连接对象。 - **数据处理**：根据获取到的信息，可以执行相应的数据库操作来获取用户设置信息。 - **响应模型**：返回一个包含用户设置信息的响应模型，便于后续的处理和使用。  ### 5. 示例代码：  ```python from fastapi import Depends, HTTPException from typing import Optional  from backend.db import Session from backend.schemas import UserSettingsResponse  async def get_user_settings(     current_user_id: str = Depends(get_current_user_id),     db: AsyncSession = Depends(get_db), ) -> UserSettingsResponse:     # 获取当前登录用户的ID和数据库连接对象     user_id = current_
- **`get_user_themes`** — [`backend/routers/users.py`](../modules/backend.md)
  _`get_user_themes` 是一个用于获取用户主题的路由函数，它返回一个包含所有用户主题的列表，并且需要依赖 `get_current_user_id` 和 `get_db` 函数来获取当前用户的 ID 和数据库会话。  这个函数的主要目的是为用户提供一个方便的方式来查看和管理用户的主题。通过调用 `get_current_user_id` 获取当前用户的 ID，可以确保只有具有该用户权限的用户才能访问这些主题信息。然后，通过调用 `get_db` 获取数据库会话，可以获取到数据库中的所有用户数据。  这个函数通常会在 `/themes` 路由中被调用，并返回一个包含所有用户主题的列表。_
- **`upsert_user_settings`** — [`backend/routers/users.py`](../modules/backend.md)
  _该函数用于更新用户设置，接收一个包含用户设置的基本数据模型和当前登录用户的ID作为参数，并返回更新后的用户设置响应模型。_
- **`_get_current_streak`** — [`backend/services/achievement_service.py`](../modules/backend.md)
  _该函数从数据库中获取当前用户的学习活动的开始时间，并返回这个时间戳。_
- **`_handle_session_complete`** — [`backend/services/achievement_service.py`](../modules/backend.md)
  _该函数用于在用户完成一个任务或活动时更新用户的成就记录，包括计算任务的持续时间，并将其存储在数据库中。_
- **`check_achievements`** — [`backend/services/achievement_service.py`](../modules/backend.md)
  _该函数用于检查用户在特定事件类型下的成就记录，包括用户是否完成了某个活动或任务。_
- **`create_access_token`** — [`backend/services/auth_service.py`](../modules/backend.md)
  _该函数创建一个访问令牌，包含用户数据和过期时间。如果提供过期时间，则将其添加到令牌中。_
- **`decode_access_token`** — [`backend/services/auth_service.py`](../modules/backend.md)
  _该函数通过JWT（JSON Web Token）验证用户身份，确保用户在登录时提供有效的凭证。如果验证成功，返回用户的payload；否则返回None。_
- **`get_password_hash`** — [`backend/services/auth_service.py`](../modules/backend.md)
  _该函数从密码中生成哈希值，用于存储和验证用户输入的密码。_
- **`verify_password`** — [`backend/services/auth_service.py`](../modules/backend.md)
  _该函数用于验证给定的密码是否与已知的哈希值匹配。_
- **`__init__`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数 `__init__` 是一个用于初始化 ChatService 类的函数，它创建了一个包含多个 `GeminiProvider` 和 `OpenAICompatibleProvider` 实例的字典 `_providers`。每个 `GeminiProvider` 实例对应一个特定的服务提供商（例如 "gemini" 或 "gpt"），并使用 `default_model` 和 `api_key_env_var` 来配置这些服务提供商。_
- **`__init__`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _`__init__` 方法用于初始化 `ChatService` 类，该类负责与 ChatGPT 进行交互。它设置了默认的模型名称为 "gemini-2.0-flash"。_
- **`__init__`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _`__init__` 是一个用于初始化 `ChatService` 类的函数，它接受多个参数，并根据这些参数设置 `base_url`、`default_model` 和 `api_key_env_var` 的值。这个函数的主要目的是在创建 `ChatService` 实例时，将这些配置信息传递给类的实例变量。_
- **`__init__`** — [`backend/services/memory_service.py`](../modules/backend.md)
  ___init__ 方法用于初始化 `MemoryService` 类，该类负责与外部服务进行交互，并管理内存数据。它通过设置 `client`、`extraction_model_name` 和 `embedding_model_name` 来配置和初始化相关的服务。_
- **`__init__`** — [`backend/services/storage_service.py`](../modules/backend.md)
  _该函数用于初始化 `StorageService` 类，它通过环境变量获取 MinIO 服务器的端点、访问密钥和秘密密钥，并将其存储在类属性中。_
- **`__init__`** — [`skills/ui-ux-pro-max/scripts/core.py`](../modules/skills.md)
  _这个函数 `__init__` 是一个用于初始化对象的函数，它接受两个参数：`k1` 和 `b`，并将其存储在类的属性中。这些参数分别表示文档的权重和文档长度。此外，该函数还创建了一个空列表 `corpus` 来存储文档内容，另一个空列表 `doc_lengths` 来存储每个文档的长度，以及一个变量 `avgdl` 用于计算平均文档长度。最后，它创建了一个空字典 `idf` 来存储每个词在文档中的频率，并且还有一个空字典 `doc_freqs` 来存储每个文档中所有词的频率。  这个函数的主要目的是为后续的文本处理和分析提供数据支持。_
