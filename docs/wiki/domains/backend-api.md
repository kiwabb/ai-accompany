# Domain: Backend API

> Auto-generated by Secrin · 2026-02-24

**Subdomain:** API

Manages data retrieval, authentication, and session management for the backend.

## Functions

- **`frontendToBackendSettings`** — [`frontend/src/api/settingsTransformer.ts`](../modules/frontend.md)
  _该函数将前端设置转换为后端配置，包括Google API密钥、AI提供者和AI模型。_
- **`AchievementToast`** — [`frontend/src/components/AchievementToast.tsx`](../modules/frontend.md)
  _`AchievementToast` 是一个 React 组件，用于显示成功或失败的成就通知。它在组件内部使用了 `AnimatePresence` 来确保动画效果生效，并且在点击按钮时触发 `onDismiss` 事件。  ### 2-3 Sentence Summary:  **What does the `AchievementToast` component do?** - 显示成功或失败的成就通知。 - 使用 `AnimatePresence` 组件确保动画效果生效。 - 在点击按钮时触发 `onDismiss` 事件。  **Why is this function important?** - 提供了一个简单的方式来通知用户成就。 - 确保用户能够及时看到成就的通知。 - 有助于提高用户体验，特别是在需要快速显示信息的场景中。_
- **`AmbientBackground`** — [`frontend/src/components/AmbientBackground.tsx`](../modules/frontend.md)
  _`AmbientBackground` 是一个 React 组件，用于在页面上显示背景图像。该组件使用了 `motion.div` 来创建一个固定且不可滚动的背景区域，并且设置了 `pointer-events-none` 属性以防止用户点击或触摸背景区域。_
