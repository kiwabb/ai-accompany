# Domain: Model Management

> Auto-generated by Secrin · 2026-02-24

**Subdomain:** Models

Handles model creation, retrieval, and management.

## Functions

- **`complete_upload`** — [`backend/routers/documents.py`](../modules/backend.md)
  _该函数用于完成上传文档，接收一个文档ID和数据库会话对象作为参数，并返回一个包含文档信息的响应模型。_
- **`delete_document`** — [`backend/routers/documents.py`](../modules/backend.md)
  _该函数用于删除指定文档，确保用户具有足够的权限进行操作。_
- **`get_document`** — [`backend/routers/documents.py`](../modules/backend.md)
  _`get_document` 是一个用于获取文档信息的路由函数，它接受一个 `document_id` 参数，并返回一个包含文档详细信息的响应模型。该函数依赖于 `AsyncSession` 和 `ge` 函数来处理数据库操作和用户身份验证。_
- **`get_upload_url`** — [`backend/routers/documents.py`](../modules/backend.md)
  _该函数用于获取上传文档的URL，通过请求参数`request`和数据库连接`db`来实现。返回一个包含文件路径和响应模型的字典。_
- **`update_document`** — [`backend/routers/documents.py`](../modules/backend.md)
  _该函数用于更新指定文档的详细信息，包括标题、内容和状态等。它接受一个 `document_id` 和一个 `update_data` 字典作为参数，并返回一个包含更新结果的响应模型 `schemas.DocumentResponse`。  ### 为什么这样做：  1. **数据验证**：首先，检查 `update_data` 是否完整且包含所有必要的字段。 2. **数据库操作**：使用 `AsyncSession` 获取数据库会话，执行更新操作。 3. **返回结果**：返回一个包含更新结果的响应模型，包括文档ID、标题、内容和状态。  ### 示例：  假设我们有一个 `DocumentUpdate` 类定义如下：  ```python class DocumentUpdate:     title: str     content: str     status: str ```  调用 `update_document` 函数时，可以这样写：  ```python async def update_document(     document_id: int,     update_data: DocumentUpdate,     db: AsyncSession = Depends(get_db) ):     # 检查更新数据是否完整     if not all(key in update_data for key in ['title', 'content', 'status']):         raise ValueError("Invalid data provided")      # 执行数据库操作     try:         await db.update(Document).where(Document.id == document_id).values(             title=update_data.title,             content=update_data.content,             status=update_data.status         ).execute()         return {"_
- **`_build_messages`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数用于构建聊天消息，根据传入的参数生成一系列的聊天消息。_
- **`list_models`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数从Google Cloud API获取模型列表，并返回一个包含模型名称的列表。如果API密钥未设置，返回空列表。_
- **`list_models`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _`list_models` 是一个抽象方法，用于从 ChatService 获取模型列表。该方法可以接受一个可选的 API 密钥参数，并返回一个包含所有可用模型名称的列表。_
- **`list_models`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数用于从API中获取模型列表，如果未提供API密钥或环境变量，则使用默认的API密钥。_
- **`list_models`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数从 `backend/services/chat_service.py` 中获取指定服务的模型列表，并返回这些模型名称。如果服务不存在，则返回空列表。_
- **`stream_chat`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _`stream_chat` 是一个用于处理聊天流的函数，它接受三个参数：消息、系统提示和聊天历史（可选）。该函数通过发送请求到外部服务来获取最新的聊天消息，并将这些消息存储在 `chat_history` 中。_
- **`stream_chat`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数 `stream_chat` 用于在聊天服务中处理消息流，并返回预设的系统提示和聊天历史。如果未提供这些参数，会返回默认值。_
- **`stream_chat`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数用于在聊天服务中发送消息，并根据传入的参数（如消息、系统提示和聊天历史）进行处理。如果未提供聊天历史，会返回一个空列表。_
- **`stream_chat`** — [`backend/services/chat_service.py`](../modules/backend.md)
  _该函数定义了一个名为 `stream_chat` 的方法，用于在聊天服务中发送消息并获取系统提示。它接受三个参数：消息、系统提示和可选的聊天历史。如果未提供聊天历史，则默认为一个空列表。这个方法通常用于处理与聊天相关的请求，并返回相应的响应。_
- **`complete_upload`** — [`backend/services/document_service.py`](../modules/backend.md)
  _该函数用于完成对指定文档的上传操作，包括更新文档的状态和关联的用户信息。_
- **`create_document`** — [`backend/services/document_service.py`](../modules/backend.md)
  _该函数用于将文件保存到指定的数据库中，并返回一个文档对象。_
- **`delete_document`** — [`backend/services/document_service.py`](../modules/backend.md)
  _该函数用于从数据库中删除指定文档，并返回删除的文档对象。_
- **`get_document`** — [`backend/services/document_service.py`](../modules/backend.md)
  _该函数从数据库中获取指定文档的信息，包括文档的ID、用户ID和文档内容。_
- **`get_documents`** — [`backend/services/document_service.py`](../modules/backend.md)
  _`get_documents` 是一个异步函数，用于从数据库中获取文档信息。该函数接受两个参数：`db` 作为 SQLAlchemy 引擎的会话对象，`user_id` 作为用户 ID。函数使用 `select` 方法来选择所有文档，并根据条件过滤出符合条件的文档。_
- **`get_upload_url`** — [`backend/services/document_service.py`](../modules/backend.md)
  _`get_upload_url` 是一个异步函数，用于生成上传文件的 URL。它接受多个参数：数据库会话 `db`、用户 ID、文件名、内容类型、标题和可选的主题 ID。该函数返回一个包含上传文件 URL 的字典。_
- **`parse_file`** — [`backend/services/document_service.py`](../modules/backend.md)
  _该函数从上传文件中读取内容，并将其转换为字符串，以便后续处理或存储。_
- **`update_document`** — [`backend/services/document_service.py`](../modules/backend.md)
  _该函数用于更新指定文档的详细信息，包括文档ID、用户ID和更新数据。_
- **`calculate_similarity_score`** — [`backend/services/embedding_utils.py`](../modules/backend.md)
  _该函数将一个浮点数转换为相似度分数，用于衡量两个向量之间的距离。具体来说，如果两个向量的余弦距离为0，则它们是相同的；如果它们的余弦距离为2，则它们是相反的。_
- **`format_memory_result`** — [`backend/services/embedding_utils.py`](../modules/backend.md)
  _该函数将一个数据库行转换为内存结果字典，包含行的ID、内容和距离属性。_
- **`generate_embedding`** — [`backend/services/embedding_utils.py`](../modules/backend.md)
  _该函数通过生成文本的嵌入，将输入的文本转换为文本向量的形式。该函数通常用于在自然语言处理任务中，如情感分析、机器翻译等。_
- **`augment_system_prompt`** — [`backend/services/memory_context_builder.py`](../modules/backend.md)
  _该函数用于增强系统提示，包括用户个人资料、内存片段和文档上下文。_
- **`build_document_context`** — [`backend/services/memory_context_builder.py`](../modules/backend.md)
  _该函数用于构建文档上下文字符串，用于在内部文档中注入特定的文本内容。_
- **`build_memory_fragments_context`** — [`backend/services/memory_context_builder.py`](../modules/backend.md)
  _构建内存片段上下文字符串用于注入系统提示，返回空字符串如果没有数据。_
- **`generate_embedding`** — [`backend/services/memory_service.py`](../modules/backend.md)
  _该函数用于生成文本的嵌入，通过阿里云提供的服务接口调用生成器来实现。_
