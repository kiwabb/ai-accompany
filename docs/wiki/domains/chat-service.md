# Domain: Chat Service

> Auto-generated by Secrin · 2026-02-24

**Subdomain:** Chat

Handles chat interactions, including uploading documents and sending messages.

## Functions

- **`chat_completions`** — [`backend/routers/sessions.py`](../modules/backend.md)
  _该函数定义了一个路由 `/chat/completions`，用于处理用户在聊天对话中的请求。它接受一个 `schemas.ChatRequest` 类型的请求体，并通过 `BackgroundTasks` 来执行一些后台任务。返回一个包含聊天结果的 JSON 数据。_
- **`chat_history`** — [`backend/routers/sessions.py`](../modules/backend.md)
  _`chat_history` 是一个用于获取聊天历史的路由函数，它接受三个参数：`limit`（限制条目数），`topic_id`（主题 ID），和 `db`（数据库会话）。该函数返回一个包含指定主题ID的历史记录。_
- **`create_learning_session`** — [`backend/routers/sessions.py`](../modules/backend.md)
  _该函数用于创建一个新的学习会，它接受一个包含课程信息的 `session_in` 参数，并将该参数传递给 `BackgroundTasks` 任务队列进行处理。返回一个包含新会信息的响应模型 `schemas.SessionResponse`，并设置 HTTP 状态码为 201（Created）。_
- **`get_daily_learning_stats`** — [`backend/routers/sessions.py`](../modules/backend.md)
  _`get_daily_learning_stats` 用于获取指定日期的每日学习统计信息，该函数依赖于 `AsyncSession` 来连接到数据库，并返回一个包含每日学习数据的响应模型。_
- **`get_learning_stats_range`** — [`backend/routers/sessions.py`](../modules/backend.md)
  _`get_learning_stats_range` 是一个用于获取学习统计范围的路由函数，它接受三个参数：`start_date`、`end_date` 和 `current_user_id`。该函数返回一个包含学习统计信息的响应模型 `StatsRangeResponse`。  这个函数的主要功能是根据传入的日期范围和当前用户ID来计算和返回学习统计数据。例如，如果传入的 `start_date` 是 2023-01-01，`end_date` 是 2023-04-01，且 `current_user_id` 是 "user123"，则函数将返回一个包含这些日期范围内的学习统计信息。  这个函数的目的是为开发人员提供一个方便的方式来获取和管理学习统计数据，以便更好地管理和分析用户的学习进度。_
